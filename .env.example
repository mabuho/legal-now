# Postgres
POSTGRES_PASSWORD=legalnow

# JWT (replace in prod — must be >=256 bits)
JWT_SECRET=change-me

# Stripe (placeholder — fill in when integrating)
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

# Mercado Pago (placeholder)
MERCADOPAGO_ACCESS_TOKEN=

# Janus
JANUS_API_SECRET=janusrocks

# Frontend env vars (passed to Vite via docker-compose webapp service)
VITE_API_BASE=http://localhost:8080/api/v1
